# Frontend API base URL.
# Keep empty to use Vite proxy (/api -> http://localhost:5000) in local dev.
# Set this only when frontend is served separately from the backend.
# Example: VITE_BACKEND_URL=https://your-backend.onrender.com
VITE_BACKEND_URL=