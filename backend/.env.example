# Gemini API key
GEMINI_API_KEY=

# Optional Gemini model overrides
GEMINI_TEXT_MODEL=gemini-2.5-flash
GEMINI_VISION_MODEL=gemini-2.5-flash

# Server port (default 5000)
PORT=5000

# Optional: chunk size for ingest (default 500)
# CHUNK_SIZE=500
# CHUNK_OVERLAP=50
